# Maelys Canonical JSON v1 reference vectors

Each `NAME.json` input canonicalizes to exactly `NAME.canonical`; optional
`NAME.indented` and `NAME.ascii` files pin the presentation forms. See
`docs/canonical-json-v1.md` for the format.

Licence: MPL-2.0 OR CC-BY-4.0 (SPDX). Independent implementations may copy
these files under CC BY 4.0 with attribution to the Maelys project.
